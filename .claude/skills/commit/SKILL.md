# Commit Skill
1. Run `git status` to see ALL changed files
2. Run `npx tsc --noEmit` to verify no type errors
3. Run `npx prisma validate` if any .prisma files changed
4. Stage ALL changed files unless user specifies otherwise
5. Write a conventional commit message summarizing changes
6. Show the full diff summary before committing
